
// ivan
import 'package:flutter/material.dart';

class AppColor {
  /// Main brand red (slightly softened)
  static const Color primary = Color(0xFFB71C1C);

  /// Lighter red for hover / accent
  static const Color primaryLight = Color(0xFFD32F2F);

  /// Darker red for pressed states
  static const Color primaryDark = Color(0xFF8E0000);

  /// App background
  static const Color background = Color(0xFFF9F9F9);

  /// Card / surface background
  static const Color surface = Color(0xFFFFFFFF);

  /// Divider / border
  static const Color border = Color(0xFFE0E0E0);

  /// Primary text (titles)
  static const Color textPrimary = Color(0xFF212121);

  /// Secondary text (subtitle, labels)
  static const Color textSecondary = Color(0xFF616161);

  /// Disabled text
  static const Color textDisabled = Color(0xFF9E9E9E);

  /// Success state
  static const Color success = Color(0xFF2E7D32);

  /// Warning state
  static const Color warning = Color(0xFFF9A825);

  /// Error / danger
  static const Color error = Color(0xFFC62828);

}